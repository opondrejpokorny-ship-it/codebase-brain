# Context save

Future context snapshots should support export first and optional persisted storage second.

Store file paths, relation evidence, warnings, depth, estimated tokens, and creation time when storage is available.
