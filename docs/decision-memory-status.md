# Decision Memory status badge

Project Decisions now shows the optional `DecisionMemory` persistence status in the page header.

The page remains local-first. The badge only shows whether the optional entity is available or whether the UI is currently using local fallback behavior.
