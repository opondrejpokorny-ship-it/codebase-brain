# Phase 49: Context Coverage

This phase adds a deterministic context coverage utility.

## Implemented

- `src/lib/contextCompletenessUtils.js`

The utility groups stored files into project categories and returns file counts, sample paths, missing categories, and an overall score.

## Why this matters

Quality scoring should explain which type of project context is missing, not only that more context is needed.
