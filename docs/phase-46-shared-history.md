# Phase 46: Shared History

This phase documents the next step for moving quality snapshots beyond browser-only state.

## Current state

Quality snapshots are available and saved locally.

## Target

Add a shared history layer later while preserving the current local fallback.

## Notes

The implementation should keep current snapshot behavior unchanged until the shared layer is available and tested.
