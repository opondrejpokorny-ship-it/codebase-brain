# Context snapshot save button

`ContextSnapshotSaveButton` is a guarded UI component for saving a built context pack into optional `ContextPack` storage. It renders nothing when the entity is not writable, preserving local/export-only behavior.
